import httpClient from './index';

describe('HttpClient instance', () => {
  describe('getInstance method', () => {
    it('should instantiate and return single instance', () => {

    });

  });

  describe('instance\'s submitEvent method', () => {
    // test throws
  });
});
